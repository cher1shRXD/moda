# Project Notes

This project is now a SwiftUI iOS app.

- Keep the product centered on "오늘 얼마까지 써도 괜찮은가?"
- Prefer local-first storage unless backend work is explicitly requested.
- APNs client registration belongs in the app, but actual push delivery requires an external APNs provider/server.
- Do not reintroduce the previous Next.js stack unless explicitly requested.
